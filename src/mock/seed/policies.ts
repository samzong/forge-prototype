import type { Policy } from '@/types'
import { daysAgo } from './time'

export const policiesSeed: Policy[] = [
  {
    id: 'pol-data-warn',
    tenantId: 'acme',
    kind: 'data',
    label: 'PII read flagged for review',
    rule: 'Read access to capabilities tagged "pii" must ship with a manual-approval gate in the manifest; otherwise warn at deploy.',
    action: 'warn',
    createdAt: daysAgo(220),
    updatedAt: daysAgo(40),
  },
  {
    id: 'pol-data-block',
    tenantId: 'acme',
    kind: 'data',
    label: 'PII export blocked',
    rule: 'Production PII must not leave VPC. Deny any deploy whose capability graph reaches an external-sink integration (feishu, webhook, email) downstream of a pii-read cap.',
    action: 'block',
    createdAt: daysAgo(210),
    updatedAt: daysAgo(12),
  },
  {
    id: 'pol-egress-warn',
    tenantId: 'acme',
    kind: 'egress',
    label: 'New external domain',
    rule: 'Warn when a handler opens a fetch() to a domain not on the tenant allowlist. Approve once, then add to allowlist.',
    action: 'warn',
    createdAt: daysAgo(180),
    updatedAt: daysAgo(6),
  },
  {
    id: 'pol-egress-block',
    tenantId: 'acme',
    kind: 'egress',
    label: 'Egress via approved proxy only',
    rule: 'All outbound HTTPS must route through the tenant proxy (proxy.acme.internal). Direct egress from runtime is denied at deploy scan.',
    action: 'block',
    createdAt: daysAgo(175),
    updatedAt: daysAgo(30),
  },
  {
    id: 'pol-cost-warn',
    tenantId: 'acme',
    kind: 'cost',
    label: 'Monthly spend > $500',
    rule: 'Projected cost (based on trigger frequency × last-7d avg runtime) over $500/month. Warn and require owner ack before deploy.',
    action: 'warn',
    createdAt: daysAgo(160),
    updatedAt: daysAgo(18),
  },
  {
    id: 'pol-cost-block',
    tenantId: 'acme',
    kind: 'cost',
    label: 'Monthly spend > $5000',
    rule: 'Projected cost over $5000/month hard-blocks deploy. Escalate to tenant admin.',
    action: 'block',
    createdAt: daysAgo(150),
    updatedAt: daysAgo(3),
  },
  {
    id: 'pol-data-warn-retention',
    tenantId: 'acme',
    kind: 'data',
    label: 'Retention window > 30d',
    rule: 'Manifest dataRetention set to "90d" or longer. Warn — retention must be justified in changelog.',
    action: 'warn',
    createdAt: daysAgo(90),
    updatedAt: daysAgo(90),
  },
]
